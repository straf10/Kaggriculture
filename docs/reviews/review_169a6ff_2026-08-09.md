# review.md — Code & Logic Review `86db6b6`…`169a6ff` (στρατηγική επανευθυγράμμιση → v1h.2)

> Ημερομηνία: **2026-08-09**. Scope: όλα τα commits και η επιχειρησιακή πρακτική **από** το
> session «Στρατηγική επανευθυγράμμιση» (2026-08-05, MASTERPLAN §3.4/§4/§5.0/§6.1/§8 + ξαναγραμμένο
> plan.md) **έως** HEAD `169a6ff` (v1h.2 α→β→γ, engine 1.32.6). Αρχεία στο εύρος:
> `agent/{executor,scheduler,planner,config,demand,constants,policy,state}.py`,
> `harness/{compare,cli,metrics,seeds,town_pin,checkpoint,report}.py`, `tests/`,
> `checkpoints/v1{c,d,e,f,g,g_1,h,h_1,h_2a,h_2b,h_2c}/`, `gates/`, docs.
> **Μόνο ανάλυση — καμία αλλαγή κώδικα σε αυτό το παραδοτέο.**
>
> Βάση σύγκρισης δεσμεύσεων: [docs/MASTERPLAN.md](../MASTERPLAN.md) §3.4/§4/§5.0/§6.1/§8 και
> [current_phase.md](../../current_phase.md) §Πρωτόκολλο. Προηγούμενα reviews στο ίδιο εύρος:
> [review_89d99f0_2026-08-05.md](review_89d99f0_2026-08-05.md),
> [review_4452427_2026-08-06.md](review_4452427_2026-08-06.md) (C1–C3 διορθώθηκαν στο `99db4fb`).
>
> Ετικέτες επαλήθευσης:
> **[CONFIRMED-READ]** = side-by-side κώδικας + artefacts στο repo·
> **[CONFIRMED-ARTEFACT]** = αριθμοί από tracked `gates/*/results.json` / `confirm_log.jsonl`·
> **[WIP]** = υπάρχει μόνο στο working tree (όχι στο `169a6ff`)· μη θεωρείται κλειδωμένο·
> **[PLAUSIBLE]** = συνεπής μηχανισμός, χωρίς ξεχωριστό repro σε αυτή τη συνεδρία.
>
> **Σημαντικό όριο HEAD vs WIP:** το working tree περιέχει ανοιχτή εργασία v1h.2d
> (`unexpected_weeds_lost`, EOD surplus/headroom sells, feed-slack PICKUP, CLI `--min-effect`).
> Τα Critical/High παρακάτω βαθμολογούνται **στο committed HEAD**, εκτός όπου σημειώνεται
> ρητά **[WIP]**.

---

## 0. Σύνοψη ευρημάτων

| Severity | # | Τι διακυβεύεται |
|---|---:|---|
| **Critical** | 4 | Ladder submissions σε νεκρό μοντέλο ζήτησης· κανένα πραγματικό harness `GO=True` ποτέ· τρύπα GO+pin· WIP μηδενίζει το practical floor |
| **High** | 8 | Order-cap reorder· BUY_LAND πριν το πλήρες κοπάδι· ledger/subset evade· GO χωρίς both_seats· raw `weeds_lost` false-FAIL· CLI forensics gap· v1c land regression παραμένει στην αλυσίδα· live≠checkpoint |
| **Medium** | 9 | EOD sell order· floor `>`/`>=`· accounting asymmetry· schedule mode· screen→top-3 unenforced· resume schedule contents· cumulative margin· projection over-reserve· shed_capacity hardcode |
| **Low** | 7 | Docs drift, help text, comment staleness, parallel persist test gap |

**Η μία γραμμή που μετράει:** η υποδομή πρωτοκόλλου (§6.1 stage↔seeds, confirm ledger, resume
contract, parallel spawn+parent jsonl) **άντεξε** μετά το `99db4fb` και το B.0′ town-pin. Το
επιχειρησιακό ρίσκο τώρα είναι αλλού: **τα ενεργά Kaggle slots (`v1g`/`v1h`) βαθμονομήθηκαν στο
1.32.5 και στο 1.32.6 αποτυγχάνουν hard metrics σε 48/48 seeds**· το καλύτερο candidate (`v1h_2c`)
είναι IMPROVED σε pinned DEV αλλά **δεν** είναι καθαρό (39 escapes, 1510 overflow)· και το
harness `go` flag **δεν έχει γίνει ποτέ `true`** στο `gates/confirm_log.jsonl`, γιατί το raw
`weeds_lost` (και τώρα escapes/overflow) μπλοκάρει κάθε confirm — άρα οι πραγματικές υποβολές
έγιναν **παρακάμπτοντας** το `GO=` σήμα με ανθρώπινη κρίση. Πριν νέο checkpoint lock ή submit:
κλείσε τις τρύπες GO (pin / seats), ολοκλήρωσε v1h.2d με καθαρό corrected gate, και μην κάνεις
commit το WIP `--min-effect default=0.0` ως έχει.

---

## 0.1 Χάρτης commits → sessions

| Commit | Ημ/νία | Session / περιεχόμενο |
|---|---|---|
| `86db6b6` | 08-06 | Infra §1.5.1–1.5.5: parallel compare, seeds, ablation, metric gates, report, replay_profile |
| `4452427` | 08-06 | v1d → v1c → v1e gates + checkpoints |
| `99db4fb` | 08-06 | Fix C1–C3/H* από review_4452427· retire ablation· CONFIRM2 |
| `7078aeb`…`68aa6aa` | 08-06 | docs/ layers· `plan.md` → `current_phase.md` |
| `a4783c8` | 08-06 | v1e submit + meta notebooks |
| `c5d62b3` | 08-06 | v1f crew 3→6 + wheat over-buy fix |
| `675712b`…`991ef74` | 08-07 | v1g 10 animals· metrics day/product· holdout reconfirm· submissions |
| *(docs/memory)* | 08-07 | v1g.1 bump 1.32.5· v1g.2 fert/sell negatives reverted· town_pin module· demand layer off |
| `5e79546` | 08-09 | B.0′ town_pin in compare· herd screen· **v1h** SW+WHEAT |
| `5d297be` | 08-09 | **v1h.1** bump 1.32.6 + constants/demand fixes |
| `169a6ff` | 08-09 | **v1h.2** D1/D3/D2 → `v1h_2a`/`_2b`/`_2c`· DEV IMPROVED, metric STOP |

---

## 0.2 Κατάσταση προηγούμενων Critical (review_4452427)

| ID | Τίτλος | Status @ `169a6ff` |
|---|---|---|
| C1 | Holdout burn / 2ο confirm | **FIXED (infra)** — ledger + `CONFIRM2_SEEDS` + `allow_repeat_confirm`. Επιχειρησιακά υπολείμματα: βλ. H3 |
| C2 | stage↔seeds → fake GO | **FIXED** — `compare()` raise· CLI conflict error |
| C3 | resume launders unmeasured metrics | **FIXED** — meta contract metrics/both_seats/steps/(αργότερα town_pin) |

`gates/gate_v1c_confirm2/results.json`: verdict **`WITHIN_MARGIN`**, mean_diff **−$298.9**,
wins 1–46, `go=false` — το v1c land increment **δεν** δικαιώθηκε σε καθαρό confirm2
[CONFIRMED-ARTEFACT]. Παρέμεινε στην αλυσίδα (v1e+ χτίζουν πάνω του)· βλ. H7.

---

## 1. CRITICAL

### C1 — Τα ενεργά ladder submissions τρέχουν σε engine που τα σκοτώνει  [CONFIRMED-ARTEFACT]

**Περιγραφή.** `v1g` (`55324447`) και `v1h` (`55383610`) βαθμονομήθηκαν στο **1.32.5**. Στο
1.32.6 (`gate_v1h1_devscreen`, memory 2026-08-09 β): ο ίδιος κώδικας `checkpoints/v1h` vs
`v1h_1` δίνει **48/48 ties** (bump ουδέτερο) αλλά
`water_weeds_lost_a=198`, `animals_escaped_a=85`, `units_sold_at_or_below_5=2370`,
`metric_gate_passed=False`, median bank ~$35.5k (από ~$42–71k). Δηλαδή τα δύο active slots
παίζουν τώρα σε καθεστώς όπου ο agent αποτυγχάνει hard metrics σε κάθε seed.

**Impact.** Κάθε νέο episode στην ladder μετά το rollout του 1.32.6 μετρά έναν agent που
τοπικά θεωρείται broken. Το BT rating θα πέσει χωρίς «νέα» αλλαγή κώδικα. Νέο submit χωρίς
καθαρό `v1h_2*` αντικαθιστά ένα slot με κάτι που μπορεί να είναι ακόμα χειρότερο.

**Οδηγίες επίλυσης.**
1. Μην κάνεις νέο submit μέχρι `metric_gate_passed=True` σε **unpinned** holdout vs `v1h_1`
   (ή νεότερο accepted baseline), με both seats.
2. Ολοκλήρωσε το τρέχον `§v1h.2d` (escapes + overflow + semantic weed gate) πριν από οποιοδήποτε
   checkpoint lock που προορίζεται για upload.
3. Κράτα ρητά ποιο fingerprint ανεβαίνει· μην ανεβάσεις live tree αν διαφέρει από το
   immutable checkpoint (βλ. H8).

**Effort:** blocked στο v1h.2d· όχι «γρήγορο hotfix».

---

### C2 — Το harness `GO=True` δεν έχει συμβεί ποτέ· οι υποβολές παρακάμπτουν το σήμα  [CONFIRMED-ARTEFACT]

**Περιγραφή.** Το `gates/confirm_log.jsonl` έχει **μηδέν** εγγραφές με `"go": true`. Κάθε
IMPROVED holdout-confirm (v1e, v1f, v1g, v1h, …) κατέληξε `go: false`, κυρίως λόγω raw
`weeds_lost` (σταθερά 8/episode × 96 orientations = 768) και αργότερα λόγω escapes/overflow.
Παρόλα αυτά έγιναν submissions με ανθρώπινη ερμηνεία «IMPROVED + τα 4 κλασικά counters καθαρά».

**Impact.** Το `GO=` έπαψε να είναι η μόνη άμυνα πριν το submit — ακριβώς το αντίθετο από το
MASTERPLAN §6.1 / plan §1.5.3 («metric gate πριν από κάθε $-gate» *και* GO μόνο από confirm).
Όταν το gate είναι μόνιμα κόκκινο για ψευδή λόγο, η ομάδα μαθαίνει να το αγνοεί· όταν γίνει
κόκκινο για αληθή λόγο (39 escapes), το ίδιο reflex είναι επικίνδυνο.

**Οδηγίες επίλυσης.**
1. Ολοκλήρωσε τη semantic διάσπαση (`unexpected_weeds_lost` hard / raw `weeds_lost` diagnostic)
   όπως ήδη προδιαγράφει το `current_phase.md` §v1h.2d — **χωρίς** να χαλαρώσεις escapes/
   overflow / water_weeds / decay.
2. Μετά το fix: **καμία** υποβολή χωρίς `GO=True` από `compare(..., stage="holdout-confirm",
   metrics=True, town_pin=None, both_seats=True)`.
3. Πρόσθεσε assertion/test: για tracked gate artefact που ισχυρίζεται acceptance, απαιτείται
   `go is True` ή ρητό `accepted_despite_go=false` στο ledger με αιτία.

**Effort:** μέρος του ήδη σχεδιασμένου v1h.2d + ~30′ policy/test.

---

### C3 — `GO` μπορεί να γίνει `True` με `town_pin` στο holdout-confirm  [CONFIRMED-READ]

**Περιγραφή.** Το πρωτόκολλο (§Πρωτόκολλο κανόνας 3, MASTERPLAN §6): pin = screening· τελικό
confirm **χωρίς** pin. Ο κώδικας μόνο προειδοποιεί:

```431:438:harness/compare.py
        if town_pin is not None:
            _warn(
                f"compare(): stage='holdout-confirm' with town_pin={town_pin!r} — ..."
            )
```

ενώ το `go` δεν ελέγχει `town_pin is None`:

```944:950:harness/compare.py
    go = bool(
        stage == "holdout-confirm"
        and verdict_allows_go
        and metrics
        and metric_gate_passed
        and not sign_test_blocks_go
    )
```

**Impact.** Ένα pinned holdout που περνά metrics μπορεί να εκτυπώσει `GO=True` για κατανομή
πόλεων που **δεν** είναι η πραγματική (ιδιαίτερα μετά το 1.32.6, όπου `basket` ≠ engine default
χωρίς pin). Silent protocol bypass.

**Οδηγίες επίλυσης.**
1. `go &= (town_pin is None)` — ή hard-raise αν `stage=="holdout-confirm" and town_pin`.
2. Test: `test_go_false_when_holdout_confirm_is_pinned`.
3. Ledger dedup: αν κρατήσεις warn-only προσωρινά, τα pinned confirms **να μην** καίνε το
   unpinned slot (σήμερα το key αγνοεί το pin — βλ. M3).

**Effort:** <30′.

---

### C4 — [WIP] CLI `--min-effect` / `--non-inferiority-margin` default `0.0` μηδενίζει το practical floor  [CONFIRMED-READ]

**Περιγραφή.** Στο **working tree** (όχι στο `169a6ff`):

```269:271:harness/cli.py
    p_compare.add_argument("--min-effect", type=float, default=0.0, ...)
    p_compare.add_argument("--non-inferiority-margin", type=float, default=0.0, ...)
```

και το `_cmd_compare` περνάει `min_effect=args.min_effect` στο `compare()`. Το API default
`None` σημαίνει `max(200, 2%·mean bank_b)`. Το `0.0` είναι *ρητό* floor μηδέν — κάθε
στατιστικά θετικό microscopic diff γίνεται `IMPROVED` / υποψήφιο GO. Υπάρχει ήδη artefact
με `min_effect_used: 0.0` σε diag run.

**Impact.** Αν αυτό μπει σε commit ως έχει, κάθε μελλοντικό gate μέσω CLI χάνει την
practical-significance άμυνα του §6 — ακριβώς το «κράτα το max από θόρυβο» που απαγορεύει το
§6.1#2.

**Οδηγίες επίλυσης.**
1. `default=None`· πέρασε στο `compare()` μόνο όταν ο χρήστης όρισε ρητά τιμή.
2. Μην χρησιμοποιείς `0.0` ως «χρησιμοποίησε default».
3. Regression test: CLI χωρίς flags ⇒ `min_effect_used >= 200`.

**Effort:** 15′. **Μην κάνεις commit το WIP CLI wiring πριν το fix.**

---

## 2. HIGH

### H1 — Truncation sort βάζει SELL τελευταία ακριβώς όταν μετράει το cap  [CONFIRMED-READ]

**Περιγραφή.** Κατασκευή: SELLs πρώτα, μετά spends. Αλλά:

```498:513:agent/executor.py
    if len(orders) > max_orders:
        ...
        orders = sorted(orders, key=_order_tier)[:max_orders]
```

με `_ORDER_TIER["SELL"]=5` (τελευταίο). Άρα ≤10: engine βλέπει SELL→spend (ίδιο-turn funding)·
>10: SELL πάει στα χειρότερα indices / κόβεται. Το D1 patch προστατεύει μόνο cashless HIRE μέσω
`slots_left`· wheat/seeds/animals/land δεν παίρνουν sell-credit και μπορεί να γίνουν silent
no-op μετά το reorder. Καταγεγραμμένο ήδη ως v1i item στη memory 08-09 β· **ακόμα ανοιχτό στο
HEAD και στο WIP**.

**Impact.** Στις πιο πολυάσχολες μέρες (ακριβώς όταν χρειάζεσαι ρευστότητα) το funding model
αντιστρέφεται. Αλληλεπιδρά άσχημα με v1h.2d EOD surplus sells που αυξάνουν τον αριθμό orders.

**Οδηγίες επίλυσης.**
1. Πάντα emit με σταθερή engine-safe σειρά: όλα τα SELL πρώτα, μετά spends· truncation **μέσα**
   σε tier (κόψε χαμηλής αξίας SELL / χαμηλής προτεραιότητας BUY), όχι cross-tier sort που
   αντιστρέφει SELL/spend.
2. Εναλλακτικά: πάντα credit queued SELL στο `available_money` *και* κράτα SELL indices πριν
   από κάθε spend.
3. Guard test: κατασκευή >10 orders που περιλαμβάνει SELL+HIRE ⇒ μετά το emit, κάθε HIRE έχει
   index > κάποιου SELL, ή budget accounting καλύπτει το cost.

**Effort:** 0.5–1 μέρα + smoke gates.

---

### H2 — `BUY_LAND` πυροδοτείται μόλις υπάρχει ≥1 ζώο ανά τύπο, όχι το πλήρες κοπάδι  [CONFIRMED-READ]

**Περιγραφή.**

```441:441:agent/executor.py
        and all(animal_placed(snapshot, name) for name in plan.animal_purchases)
```

και `animal_placed` = `placed_count > 0` (ρητά τεκμηριωμένο στο docstring ως «some
investment», όχι full herd). Με targets `{4C,6S}` η γη (NE/SW, $1k/$2k + `min_reserve`) μπορεί
να αγοραστεί ενώ απομένουν έως 8 ζώα — ακριβώς το cash-starvation μοτίβο που σκότωσε το πρώτο
v1c.

**Impact.** Πρώιμο BUY_LAND ανταγωνίζεται wheat/animal buys· σε στενά πρωινά ταμείου αυξάνει
κίνδυνο under-feed / under-hire. Δεν είναι «ατεκμηρίωτο bug» — είναι συνειδητή χαλαρή πύλη που
ξανάνοιξε μετά την κλίμακα ζώων.

**Οδηγίες επίλυσης.**
1. Άλλαξε σε `placed_count(name) >= plan.animal_purchases[name]` (ή in-flight ≥ target).
2. Dev-screen occupancy με town-pin basket vs τρέχον· μέτρησε escapes και day-2 weeds.
3. Ενημέρωσε το docstring ώστε να μην λέει δύο διαφορετικά πράγματα από τον κώδικα στόχο.

**Effort:** μικρή αλλαγή + 48-seed screen.

---

### H3 — Confirm ledger καίει / επιτρέπει πολλαπλά «ματιά» στο holdout  [CONFIRMED-READ]

**Περιγραφή.**
1. Κάθε `stage="holdout-confirm"` γράφεται στο ledger **μετά** το run — ακόμα κι αν
   `metrics=False`, incomplete, ή `metric_gate_passed=False`.
2. Το stage check δέχεται **οποιοδήποτε subset** των CONFIRM sets· το ledger label για
   ad-hoc `100-111` γίνεται `custom[...]` και **δεν** συγκρούεται με `"holdout"`.

**Impact.** (1) Τυχαίο unmetered confirm μπλοκάρει το πραγματικό χωρίς
`--allow-repeat-confirm`. (2) Πολλά overlapping subsets = πολλαπλά looks στο holdout —
ακριβώς το C1-class risk που το ledger υποτίθεται ότι κλείνει.

**Οδηγίες επίλυσης.**
1. Append ledger μόνο όταν `metrics and not incomplete` (ή ξεχωριστό `attempted` vs
   `counted_look`).
2. Για `holdout-confirm` απαιτείται **ακριβές** named set (πλήρη 48), όχι proper subset —
   αλλιώς raise ή αναβάθμιση σε `dev-screen`.
3. Test και για τα δύο.

**Effort:** 1–2 ώρες.

---

### H4 — `GO` δεν απαιτεί `both_seats=True`  [CONFIRMED-READ]

**Περιγραφή.** Το πρωτόκολλο λέει both seats πάντα για GO. Το CLI έχει `--single-seat`· το
`go` δεν το αποκλείει. Default είναι both seats, άρα το bug είναι opt-in — αλλά silent.

**Impact.** Weed/seat asymmetry μπορεί να κρύψει regressions· underpowered confirm φαίνεται
ως GO.

**Οδηγίες επίλυσης.** `go &= both_seats` ή raise στο holdout-confirm με `both_seats=False`.
Test αντίστοιχο.

**Effort:** 15′.

---

### H5 — Στο HEAD το hard gate απαιτεί raw `weeds_lost==0` (μόνιμο false-FAIL)  [CONFIRMED-READ] [CONFIRMED-ARTEFACT]

**Περιγραφή.** @ `169a6ff`:

```text
metric_gate_passed = (... and shed_overflow_burnt_a == 0 and weeds_lost_a == 0 ...)
```

Το `extract_metrics()` μετρά κάθε PLANT→WEED transition, συμπεριλαμβανομένων successful
ongoing-crop harvest retirements (~8/episode σταθερά από v1f+). Το `current_phase.md` §v1h.2d
το αναγνωρίζει και σχεδιάζει `unexpected_weeds_lost` — **υπάρχει στο WIP, όχι στο HEAD**.

**Impact.** Δεν είναι false-PASS· είναι **μόνιμο false-FAIL** που (α) κάνει το `GO=` άχρηστο
(C2) και (β) μπερδεύει diagnostics (`gate_v1h_holdout`: IMPROVED +$2835 αλλά
`metric_gate_passed=false` λόγω weeds, με tiny overflow=20). Αν κάποιος «χαλαρώσει» το gate
χωρίς semantic split, ανοίγει πραγματικό silent PASS.

**Οδηγίες επίλυσης.** Ακολούθησε το ήδη γραμμένο §v1h.2d checklist· μην βάζεις
`weeds_lost<=8` exception.

**Effort:** ήδη στο πλάνο.

---

### H6 — Tracked `results.json` στο HEAD παραλείπει τους counters που αποτυγχάνουν το gate  [CONFIRMED-ARTEFACT]

**Περιγραφή.** Στο HEAD, `_results_json_dict` σειριοποιεί μόνο τα αρχικά τέσσερα counters.
`gates/gate_v1h_holdout/results.json` έχει `metric_gate_passed: false` αλλά **top-level**
`weeds_lost_a` / `shed_overflow_burnt_a` = απόντα (`None` στο load)· τα νούμερα ζουν μόνο μέσα
στα `per_seed` rows. Το WIP ήδη προσθέτει τα πεδία.

**Impact.** Silent forensics failure: διαβάζοντας το summary νομίζεις ότι δεν ξέρεις *γιατί*
έπεσε το gate. Όχι silent PASS, αλλά σπάει την παρατηρησιμότητα §8.

**Οδηγίες επίλυσης.** Serialize όλα τα gate inputs + `incomplete` + `warnings`. Ενημέρωσε CLI
help που ακόμα λέει «four == 0».

**Effort:** 20′ (εν μέρει ήδη στο WIP).

---

### H7 — Το v1c παραμένει στην αλυσίδα ως μετρημένη μικρή παλινδρόμηση  [CONFIRMED-ARTEFACT]

**Περιγραφή.** Μετά το C1 fix, `CONFIRM2` έδωσε `WITHIN_MARGIN` −$299 (1–46 wins) vs v1d.
Δεν έγινε revert της γης· v1e/v1f/v1g/v1h χτίζουν πάνω σε NE+ animals. Το απόλυτο Phase-1 vs
starter παραμένει τεράστιο θετικό — αλλά το **οριακό** κόστος της γης ποτέ δεν καθαρίστηκε με
GO.

**Impact.** Κάθε μελλοντικό «πόσο αξίζει άλλο quadrant» screen κουβαλά baseline που ήδη πληρώνει
άγνωστο land tax. Όχι άμεσο submit blocker, αλλά μολύνει ερμηνείες margin.

**Οδηγίες επίλυσης.** Καταγραμμένο ως known debt στο current_phase· αν ξαναγγίξεις land/crew,
χρησιμοποίησε φρέσκο confirm set (όχι 100–147 / 200–247 για το ίδιο ερώτημα). Μην ξανατρέξεις
confirm2 για το ίδιο v1c fingerprint χωρίς `allow_repeat_confirm` και νέα υπόθεση.

**Effort:** documentation / επόμενο land increment.

---

### H8 — Live `agent/` μπορεί να αποκλίνει από το τελευταίο immutable checkpoint  [CONFIRMED-READ]

**Περιγραφή.** `checkpoints/v1h_2c` είναι το τελευταίο κλειδωμένο candidate. Το working tree
έχει ήδη v1h.2d αλλαγές σε `executor.py` / `scheduler.py` (EOD surplus, headroom projection,
WHEAT PICKUP slack) πάνω από config identical με `v1h_2c`. Fingerprints checkpoint
`v1h_1`…`v1h_2c` επαληθεύονται· το live tree **δεν** είναι byte-identical με `v1h_2c`.

**Impact.** Submit/packaging από `.` αντί από checkpoint path στέλνει μη-gated κώδικα.
Επαναλαμβάνει το pattern του 08-06 regression έναντι v1b.

**Οδηγίες επίλυσης.**
1. Κάθε upload **μόνο** από `checkpoints/<ver>/` μετά από fingerprint match.
2. Πριν gate: `diff` live vs baseline checkpoint· αν WIP, τρέξε gates στο WIP *ρητά* ως νέο
   increment, μην το βαφτίσεις `v1h_2c`.

**Effort:** process· 0 κώδικας.

---

## 3. MEDIUM

### M1 — EOD headroom/surplus πουλάει υψηλής αξίας προϊόντα πρώτα  [CONFIRMED-READ] [WIP+HEAD paths]

Η επανάληψη `sell_products` (STRAWBERRY→…→MILK) στο EOD surplus/headroom (WIP) / γενικό sell
loop σημαίνει ότι σε overflow χτυπάς φράουλες πριν το glut MILK του 1.32.6. **Remediation:**
ταξινόμηση υποψηφίων overflow κατά χαμηλό $/unit ή χαμηλή NPC ζήτηση πρώτα.

### M2 — Ασυνέπεια floor comparator `>` vs `>=`  [CONFIRMED-READ]

Normal/liquidation: `price > floor`· EOD paths στο WIP: `>= hard_floor`. Μονάδες ακριβώς $5
μπορούν να πουληθούν στο EOD και να απορριφθούν στη liquidation — υπονομεύει το D3 story.
**Remediation:** μία predicate παντού· match το D3 test.

### M3 — Ledger dedup αγνοεί `town_pin`  [CONFIRMED-READ]

Key `(agent_b_fp, seed_set_name)` μόνο. Pinned confirm καίει το unpinned slot. Λύνεται μαζί με
C3 (καλύτερα: raise στο pin+holdout).

### M4 — `--town-pin schedule` παραμένει first-class μετά το 1.32.6  [CONFIRMED-READ]

`schedule` ≈ 0.24% πραγματικών πόλεων. Το v1h′ dev-screen χρησιμοποίησε `schedule`· το holdout
χωρίς pin έσωσε το GO-claim εκεί, αλλά μελλοντικά occupancy screens με `schedule` είναι
παραπλανητικά. **Remediation:** warn/deprecate στο compare· docs ήδη λένε basket-only.

### M5 — Screen→confirm / «ποτέ max-of-k» δεν επιβάλλεται στον κώδικα  [CONFIRMED-READ]

Μόνο το `go` flag απαιτεί holdout-confirm. Το §6.1#2 (top-3 → μία confirm) είναι human-process.
Τα herd/SW smoke sweeps (4-seed, πολλά variants) είναι ακριβώς το risk surface. **Remediation:**
τουλάχιστον ledger rule «candidate fp πρέπει να εμφανίζεται σε prior tracked dev-screen».

### M6 — Resume δεν επαληθεύει περιεχόμενα `town_schedules`  [CONFIRMED-READ]

Ελέγχει μόνο το mode. Αλλαγή αλγορίθμου `basket_for` μπορεί να αναμίξει κατανομές κάτω από το
ίδιο resume. **Remediation:** hash των schedules στο meta contract.

### M7 — Cumulative NON_INFERIOR margin budget ακόμα ανύπαρκτο  [CONFIRMED-READ]

`min_effect_used` καταγράφεται (μερικό fix H1 του παλιού review)· δεν υπάρχει budget κατά μήκος
αλυσίδας increments. Αλυσίδα μικρών NON_INFERIOR τρώει bank. Ανοιχτό από review_4452427 §7.2.

### M8 — `_projected_near_term_incoming` υπερ-δεσμεύει (WIP)  [PLAUSIBLE]

Finite crops στο `max_yield`, animals full on-tile + horizon fertilizer· καθισμένο
`fertilizer_available` μπορεί να αγνοείται. Φουσκώνει headroom → επιπλέον fire-sales → τάση
προς H1 cap. **Remediation:** μέτρησε vs πραγματικό EOD stock σε 4 seeds πριν κλειδώσεις v1h.2d.

### M9 — `shed_capacity = 100` hardcoded στον executor  [CONFIRMED-READ]

Αν το engine json αλλάξει, το headroom λογισμικό σιωπά λάθος. Διάβασε από configuration.

---

## 4. LOW

| ID | Θέμα | Σημείωση |
|---|---|---|
| L1 | CLI `--metrics` help ακόμα «four == 0» | HEAD· WIP μερικώς ενημερωμένο |
| L2 | PASTURE config comments ακόμα «8 COW / 5 SHEEP» | Μετά D2 είναι 4/6 carve |
| L3 | `planner.py` σχόλιο «liquidation ignores floors» | Ψευδές μετά D3 |
| L4 | `KAGGRI_ABLATION` holdout raise vestigial | Harmless· ablation retired |
| L5 | Parallel incremental persist χωρίς dedicated test | Κώδικας OK στο `as_completed`· test gap από παλιό H6 |
| L6 | Demand model live αλλά policy-idle | By design (`dynamic_sell_floor=False`)· OK για v1i |
| L7 | `opponent_tiles` unused | Reserved· OK |

---

## 5. Συμμόρφωση με MASTERPLAN §3.4 / §4 / §5.0 / §6.1 / §8

| Δέσμευση | Κατάσταση @ HEAD / πρακτική |
|---|---|
| §5.0#1 Ξεμπλοκάρισμα −$2.195 via ablation | **Έγινε** τότε· ablation **αποσύρθηκε** στο `99db4fb` αφού ξεμπλοκάρισε — OK |
| §5.0#2 Parallel compare + dev/holdout | **Έγινε**· workers identity test υπάρχει |
| §5.0#3 Gap analysis raw replays | **Έγινε** (`replay_profile` / derived profiles) |
| §5.0#4 Episode report + receipts | **Έγινε** (`harness/report.py`)· W&B απόρριψη χρήστη |
| §5.0#5 v1c/v1d capacity | **Έγινε** με γνωστό v1c debt (H7) |
| §5.0#6 BBO | **Σωστά όχι ακόμα** (μετά features· §3.4) |
| §5.0#7 RL | **Σωστά όχι** (§4 triggers unmet) |
| §6.1#1 Dev/holdout split | **Enforced** raise· subsets confirm ακόμα τρύπα (H3) |
| §6.1#2 Screen→confirm, ποτέ max-of-k | **GO flag OK**· top-3 **όχι** coded (M5) |
| §6.1#3 Parallel correctness | **OK** (spawn, parent jsonl, fp before dispatch) |
| Metric gate πριν $-gate | **AND στο `go`**· $-verdict ακόμα υπολογίζεται όταν metrics fail (OK)· ορισμός weeds λάθος (H5) |
| Ablation all-off ⇒ diff 0 | **N/A** — tool retired |
| §8 Observability | Report υπάρχει· tracked gates μερικές φορές χωρίς failing counters (H6) |
| Town-pin πρωτόκολλο | CM μέσα στον worker **OK**· GO+pin **όχι** (C3) |

**Αποκλίσεις υλοποίησης από το πλάνο που ήταν συνειδητές/μετρημένες (όχι bugs):** v1g.2
sell-floor OFF· fertilizer-timing reverted· shed-access nearest reverted· herd έμεινε
`{6C,4S}` μέχρι v1h.2 D2· crew peak 6→10 μόνο στο SW παράθυρο.

---

## 6. Pre-Submission / Pre-Checkpoint Checks

Πριν θεωρηθεί ασφαλές **νέο immutable checkpoint** ή **Kaggle submit** πάνω σε αυτή τη βάση:

### A. Harness / πρωτόκολλο (μπλοκάρει GO-laundering)

- [ ] **A1.** `go` απαιτεί `town_pin is None` (C3) — merged + tested.
- [ ] **A2.** `go` απαιτεί `both_seats=True` (H4).
- [ ] **A3.** Holdout-confirm δέχεται μόνο πλήρες named confirm set, όχι subset (H3).
- [ ] **A4.** Ledger append μόνο για metered complete confirms (H3).
- [ ] **A5.** CLI `min_effect`/`non_inferiority_margin` default `None`, όχι `0.0` (C4) — **πριν
      οποιοδήποτε commit του WIP cli.py**.
- [ ] **A6.** `_results_json_dict` περιλαμβάνει όλα τα gate counters + warnings (H6).

### B. Agent correctness στο 1.32.6

- [ ] **B1.** v1h.2d: `unexpected_weeds_lost==0` hard· raw `weeds_lost` diagnostic only (H5/C2).
- [ ] **B2.** `animals_escaped==0`, `shed_overflow_burnt==0`, `water_weeds_lost==0`,
      `plant_decay_units_lost==0`, ≤$5 budget ≤2%, no market abort.
- [ ] **B3.** Order emit/truncation δεν αντιστρέφει SELL κάτω από spends όταν `len>10` (H1) —
      τουλάχιστον smoke· ιδανικά πριν το submit.
- [ ] **B4.** Αξιολόγηση H2 (full-herd before BUY_LAND) αν escapes/overflow παραμένουν μετά B2.
- [ ] **B5.** `pytest tests/` πράσινο στο **ίδιο** tree που θα παγώσει.

### C. Gate protocol (μία φορά, καθαρά)

- [ ] **C1.** DEV screen 0–47, both seats, `--town-pin basket --metrics` vs accepted baseline
      (`v1h_1` ή νεότερο locked)· verdict IMPROVED ή NON_INFERIOR **και**
      `metric_gate_passed=True`.
- [ ] **C2.** Αν C1 καθαρό: **ένα** unpinned holdout-confirm 100–147, both seats, `--metrics`,
      `town_pin=None`· χωρίς `--allow-repeat-confirm` εκτός αν το ledger το απαιτεί για
      τεκμηριωμένο λόγο.
- [ ] **C3.** Απαίτηση `GO=True` στο stdout **και** στο `gates/confirm_log.jsonl` πριν το
      packaging (C2).
- [ ] **C4.** Μην κάνεις tuning από το holdout αποτέλεσμα· `INCONCLUSIVE`/`REGRESSED` = STOP.

### D. Packaging / reproducibility

- [ ] **D1.** Freeze `checkpoints/<ver>/` από το ακριβές gated tree· επαλήθευσε
      `agent_fingerprint` vs manifest.
- [ ] **D2.** Upload **μόνο** από το checkpoint staging (όχι live `.`) — H8.
- [ ] **D3.** `requirements-dev.txt` pin == εγκατεστημένο == `engine_reference` tripwire
      (`1.32.6` σήμερα).
- [ ] **D4.** Submission checklist §Α.2: format, loader last-callable, timing, determinism,
      mirror smoke, `KAGGRI_DEBUG` off.
- [ ] **D5.** Θυμήσου: Kaggle κρατά **μόνο 2** active — νέο upload αντικαθιστά· μην κάψεις το
      καλύτερο slot με half-fixed agent (C1).

### E. Ρητά ΜΗ κάνε τώρα

- [ ] Μην υποβάλεις `v1h_2c` ως έχει (39 escapes / 1510 overflow).
- [ ] Μην υποβάλεις `v1h`/`v1g` ξανά «για refresh» χωρίς 1.32.6-clean gate.
- [ ] Μην κάνεις BBO / RL / portfolio rebalance προς meta fingerprints (§3.4 / ⚠️γ).
- [ ] Μην χαλαρώσεις hard gates με `weeds_lost<=8` exception.

---

## 7. Self-check (δεύτερο πέρασμα)

Ελέγχθηκαν τα ευρήματα ενάντια σε git show HEAD, artefacts, και working-tree diff ώστε να μην
μείνουν false positives ούτε να αγνοηθούν κρίσιμα:

| Ισχυρισμός | Αποτέλεσμα self-check |
|---|---|
| C1–C3 του 08-06 ακόμα σπασμένα; | **Όχι** — διορθωμένα στο `99db4fb` και κρατούν στο HEAD |
| Ablation all-off self-test λείπει ως Critical; | **False positive αν αναφερθεί έτσι** — το tool αποσύρθηκε σκόπιμα· commitment N/A |
| `GO`+pin ήδη εκμεταλλεύτηκε; | **Όχι** — τρύπα υπάρχει· confirm_log δείχνει `town_pin: null` στο v1h holdout |
| Raw weeds = silent PASS; | **Αντίστροφο** — είναι silent/μόνιμο FAIL· severity High/Critical μέσω C2, όχι ως false GO |
| Order-sort διορθώθηκε στο v1h.2; | **Όχι** — ακόμα `if len > max: sorted(...tier)` |
| D1/D2/D3 λείπουν από το tree; | **Όχι** — παρόντα στο `v1h_2c` / live |
| 1.32.6 constants breakage ακόμα ζωντανό; | **Όχι** — διορθώθηκε στο v1h.1 (`_const`, demand interval 24) |
| `unexpected_weeds` ήδη στο HEAD; | **Όχι** — μόνο WIP· μην το πιστώνεις στο `169a6ff` |
| `min_effect=0.0` στο HEAD CLI; | **Όχι** — HEAD CLI δεν περνάει τα flags· **WIP μόνο** (C4) |
| `animal_placed>0` είναι ατύχημα; | **Μερικώς** — docstring το δηλώνει· παραμένει High risk μετά την κλίμακα ζώων |
| Parallel compare non-deterministic; | **Δεν βρέθηκε** — parent jsonl + spawn CM-in-worker· identity test υπάρχει |
| Κάποιο confirm είχε `go:true`; | **Όχι** — μηδέν στο ledger |
| v1h holdout ήταν pinned GO; | **Όχι** — unpinned IMPROVED, `go:false` λόγω metrics |
| Live == `v1h_2c`; | **Όχι** — WIP executor/scheduler μπροστά |

**Αλλαγές severity μετά το self-check:** το raw `weeds_lost` υποβαθμίστηκε από «Critical false
PASS» σε **H5 + καύσιμο για C2** (σωστό failure mode)· το WIP `min_effect` ανέβηκε σε
**Critical** μόνο ως pre-commit blocker· το ablation gap αφαιρέθηκε από Critical.

---

## 8. Τι φαίνεται στερεό (μην ξανανοίξεις χωρίς νέα ένδειξη)

- Stage↔seeds validation + CLI conflict errors.
- Confirm ledger + `allow_repeat_confirm` + sign-test GO block + WITHIN_MARGIN override.
- Resume contract: fingerprints, metrics, both_seats, steps, town_pin mode.
- Parallel path: `mp_context="spawn"`, town pin ανοίγει μέσα στο `_play_orientation`, jsonl από
  parent στο `as_completed`, fingerprints πριν το pool.
- Metric seat mapping A@0/A@1· counters summed not averaged· `None` όταν `metrics=False`.
- Checkpoint fingerprint/manifest immutability guard.
- v1h.1 per-symbol constants + demand flat×1 / interval 24.
- Wheat over-buy fix (unfed_animals + remaining targets) ακόμα στη θέση του.
- FEED priority −1 για `consecutive_unfed>=1`· D1/D3/D2 guards.
- Engine tripwire tests + `requirements-dev.txt` pin 1.32.6.

---

*Τέλος review `169a6ff` / 2026-08-09.*
